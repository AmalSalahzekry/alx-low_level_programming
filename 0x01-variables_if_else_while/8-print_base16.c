#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints all the numbers of base 16 in lowercase
 *
 * Return: 0 (success)
*/

int main(void)
{
	int num = 48;

	while (num <= 57)
	{
		putchar(num);
			num++;
	}
	int ch = 97;

	while (ch <= 102)
	{
		putchar(ch);
		ch++
	}
	putchar('\n');
		return (0);
}
